import { UserSlicerPipe } from './user-slicer.pipe';

describe('UserSlicerPipe', () => {
  it('create an instance', () => {
    const pipe = new UserSlicerPipe();
    expect(pipe).toBeTruthy();
  });
});
